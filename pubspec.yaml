name: animation_tools
description: >-
  A standardized command-line Dart tool for processing animations. Supported now: Spine.
repository: https://github.com/signmotion/animation_tools
topics:
  - animation
  - tools
  - transform
  - rename
  - copy

# Format:
#   version: major.minor.patch
# You can calculate the number version code by this formula:
#   code = m[A]jor * 100 * 100 + m[I]nor * 100 + [P]atch
# Otherwise: +AAIIPP
version: 0.2.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  image: ^4.1.7
  json_dart: 0.1.1
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.2
