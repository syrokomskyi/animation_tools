name: animation_tools
description: >-
  A standardized command-line easy-to-use Dart tool for processing animations.
  Supported now: Spine.
repository: https://github.com/signmotion/animation_tools
topics:
  - animation
  - cli
  - tools
  - transform
  - rename

# Compatible with https://semver.org
# Format:
#   version: major.minor.patch
# You can calculate the number version code by this formula:
#   code = m[A]jor * 100 * 100 + m[I]nor * 100 + [P]atch
# Otherwise: +AAIIPP
version: 0.2.2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0
  image: ^4.1.7
  json_dart: ^0.1.3
  meta: ">=1.10.0 <3.0.0"
  path: ">=1.8.0 <3.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.4
